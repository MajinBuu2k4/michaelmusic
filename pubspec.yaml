name: michaelmusic
description: "App Nghe Nhạc Của Mai Cồ"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Icon mặc định
  cupertino_icons: ^1.0.6

  # --- Đồ chơi mới cho Mai Cồ ---
  just_audio: ^0.9.37
  audio_session: ^0.1.19
  dio: ^5.4.3
  path_provider: ^2.1.3
  rxdart: ^0.27.7
  audio_video_progress_bar: ^2.0.2

  # 🔥 CHỈ CẦN DÒNG NÀY LÀ ĐỦ (Đã bao gồm thông báo)
  audio_service: ^0.18.18

  # Permission Handler (Bản mới nhất OK)
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

# Cấu hình Logo
flutter_launcher_icons:
  android: "ic_launcher"
  image_path: "assets/icon-app_256.png"
  min_sdk_android: 21

# Cấu hình Flutter
flutter:
  uses-material-design: true

  # Assets
  assets:
    - assets/michaelsongs.json
    - assets/itunes_256.png
    - assets/icon-app_256.png